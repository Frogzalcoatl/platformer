Improve sizing and coloring of mobile ui.

Determine whether ui funcs can be optimized at all (they probably can be)

Account for polygon radius in draw func

Use enet lib for online multiplayer

Render textures in chunks

Use a texture atlas

Figure out some way to automatically load static entities on exposed solid tiles.
